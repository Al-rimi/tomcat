/**
 * Instance source marker type
 *
 * Describes whether a Tomcat instance is managed by the extension
 * or discovered as an external process.
 *
 * @type {'managed' | 'external'}
 */
export type InstanceSource = 'managed' | 'external';
